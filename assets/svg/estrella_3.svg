<svg width="120" height="120" viewBox="0 0 120 120" xmlns="http://www.w3.org/2000/svg">
  <!-- Línea vertical (lisa) -->
  <path d="M60 5 L60 115"
        fill="none"
        stroke="#94bbce"
        stroke-width="6"
        stroke-linecap="round"/>

  <!-- Línea horizontal (lisa) -->
  <path d="M5 60 L115 60"
        fill="none"
        stroke="#94bbce"
        stroke-width="6"
        stroke-linecap="round"/>

  <!-- Diagonal ↘︎ (punteada, puntos más separados) -->
  <path d="M30 30 L90 90"
        fill="none"
        stroke="#94bbce"
        stroke-width="6"
        stroke-linecap="round"
        stroke-dasharray="8 8"/>

  <!-- Diagonal ↗︎ (punteada, puntos más separados) -->
  <path d="M90 30 L30 90"
        fill="none"
        stroke="#94bbce"
        stroke-width="6"
        stroke-linecap="round"
        stroke-dasharray="8 8"/>
</svg>
